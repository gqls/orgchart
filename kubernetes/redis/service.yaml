# orgchart-redis-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: orgchart-redis
spec:
  selector:
    app: orgchart-redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP